# As long as the dependency is available in Bazel Central Registry, https://registry.bazel.build/, you can simply depend on it with a bazel_dep directive.

bazel_dep(name = "googletest", version = "1.14.0")